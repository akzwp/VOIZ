# 📋 گزارش کامل بازطراحی UI پنل VOIZ

> **نسخه ۷.۱ (به‌روزرسانی):** رفع ۱۶ مشکل UI/UX گزارش‌شده — تمام تغییرات فقط لایه ظاهری.
>
> | # | بخش | فیکس |
> |---|---|---|
> | ۱ | گزارش پیشرفته صف (Asternic) | `ui/embedded.css` — بازطراحی کامل `#wrapper/#left/#right/#rest/#footer`، چندآیزل‌ها، ریسپانسیو |
> | ۲ | ریز مکالمات / مکالمات ضبط شده | بازساخت toolbar های DataTables (`dt-buttons`، فیلتر، صفحه‌بندی) بدون تداخل |
> | ۳ | ضربدر کنار لوگو + پاپ‌آپ تغییر رمز | X فقط در حالت drawer موبایل؛ مودال همیشه مرکز صفحه (`patchModalCentering`)؛ منوی ادمین استاندارد |
> | ۴ | GeoIP Map تولتیپ | مهار تولتیپ داخل ویوپورت (CSS + `initMapTooltips`) |
> | ۵ | تنظیمات تلفنی + کنتراست متن | بخش PBX در `embedded.css` + خنثی‌سازی رنگ‌های hardcoded تیره/روشن |
> | ۶ | سرور DHCP ورودی IP | اکتت‌های ۳رقمی با پرش خودکار، نقطه، paste کامل (`initOctets` + CSS) |
> | ۷ | دیتای میزکار | قالب‌های `dashboard` و `monitoring` به مارکاپ اصلی ایزابل برگشت؛ گرافیک فقط با CSS (`table#applet_grid`) |
> | ۸ | دفترچه تلفن افزودن آیتم | فرم `tabForm` استاندارد: لیبل/فیلد هم‌تراز، عرض کنترل‌شده |
> | ۹ | تقویم + جلالی | استایل FullCalendar + کارت تقویم جلالی (`initJalali`) کنار تقویم میلادی |
> | ۱۰ | مونیتورینگ تماس | تم‌کردن `.pbxpanel/.pbxelement/.pbxtooltip` |
> | ۱۱ | FOP2 Manager / پنل اپراتور | بازطراحی embedded سایدبار/کارت/هدر فورک fop2 |
> | ۱۲ | یافتن سخت‌افزار | کنتراست متن‌ها و fieldset های تمیز در `.voiz-hardware` |
> | ۱۳ | QR Configurations | مهار سرریز هدرها/بدنه‌ها در صفحات فریم‌شده |
> | ۱۴ | تنظیم گوشی‌ها | جدول‌های embedded با padding/border یکنواخت و بدون تداخل |
> | ۱۵ | افزونه‌ها (flexigrid) | بازطراحی `.flexigrid` (toolbar/hDiv/bDiv/pDiv) |
> | ۱۶ | رادیوباتن/سوییچ (Beta Channel) | ری‌دیزاین سراسری checkbox/radio/switch با CSS خالص |
>
> تغییرات: `ui/embedded.css` (جدید)، `ui/components.css`، `ui/shell.css`، `js/voiz-ui.js`، `js/voiz-embedded.js` (جدید)، `theme/pbxconfig/footer_content.php`، بازگردانی ۶ قالب dashboard/monitoring، نسخه کش `v=7.1.0`.

---
# 📋 گزارش کامل بازطراحی UI پنل VOIZ

> گزارش نهایی کاستومایز تم `vitenant` ایزابل — تمام موارد خواسته‌شده (۱ تا ۱۳) به‌علاوه بررسی و رفع باگ‌های پنهان UI/UX.
> **هیچ فایل PHP یا منطقی دستکاری نشده است.** تمام تغییرات فقط در لایه‌ی ظاهری (CSS / JS رابط کاربری / Template HTML) است.

---

## 🎯 معماری کلی راهکار

تم جدید به‌صورت یک **لایه‌ی بازطراحی مستقل** ساخته شد که **آخرِ از همه** فایل‌های CSS قدیمی (neon، purple، bootstrap-rtl و…) لود می‌شود و همه‌چیز را با اولویت بالاتر بازنویسی می‌کند:

| فایل | نقش |
|---|---|
| `css/voiz-ui.css` (~۲۶۰۰ خط) | کل سیستم طراحی: توکن‌های رنگ، تایپوگرافی، سایدبار، تاپ‌بار، فرم‌ها، جدول‌ها، مودال‌ها، ریسپانسیو |
| `js/voiz-ui.js` (~۲۴۰ خط) | سوییچ تم (localStorage)، دراور موبایل، جستجوی زنده‌ی ماژول‌ها، نمایش/مخفی رمز، بستن پاپ‌آپ با ESC |
| `_common/*.tpl` | فقط مارک‌آپ: تاپ‌بار جدید، فرم لاگین جدید، متادیتای تم |
| `fonts/vazirmatn/*.woff2` | ۸ وزن فونت وزیرمتن (لود لوکال، بدون CDN) |

تم با `data-theme` روی تگ `<html>` عوض می‌شود و **قبل از رندر** از localStorage خوانده می‌شود (پس فلشِ تم اشتباه هنگام لود وجود ندارد). همه‌ی رنگ‌ها با CSS Variables تعریف شده‌اند.

---

## ✅ موارد ۱ تا ۱۲ (طبق دستور)

### ۱. راست‌چین کامل + فونت وزیرمتن
- `direction: rtl` و `text-align: right` روی کل پنل
- ۸ وزن فونت **Vazirmatn** به‌صورت لوکال با `font-display: swap`
- جزیره‌های LTR هوشمند: مسیرها (`.confdir`)، اعداد، فیلدهای number و tel

### ۲. تم دارک و لایت (دقیقاً با پالت خواسته‌شده)
| تم | پس‌زمینه | اکسنت اصلی | ثانویه |
|---|---|---|---|
| 🌙 **دارک** | مشکی گرافیتی | نارنجی تیره | طوسی |
| ☀️ **لایت** | سفید استخوانی مایل به طوسی | نارنجی تیره | آبی تیره |

- سوییچ تم در تاپ‌بار و صفحه‌ی لاگین (آیکون ماه/خورشید)
- همه‌ی کامپوننت‌ها (فرم، جدول، مودال، dropdown و…) از هر دو تم پیروی می‌کنند

### ۳. فرم‌های استاندارد و ریسپانسیو
- ارتفاع یکسان ۳۴px برای همه‌ی input/select، فوکوس‌رینگ نارنجی، placeholder ملایم
- فلش سفارشی select که **با RTL نمی‌افتد** (فلشِ پیش‌فرض مرورگر حذف شد)
- فیلدهای غیرفعال/readonly با استایل متمایز
- چیدمان فرم‌ها در موبایل تک‌ستونه می‌شود

### ۴. کانتنت ریسپانسیو منوها
- جدول‌ها با اسکرول افقی نرم در موبایل، toolbar جدول‌ها flex-wrap می‌شوند
- `min-width:0` و حذف عرض‌های ثابت قدیمی (مثلاً `width:1245px` قدیمی) که باعث شکستگی در مانیتورهای مختلف می‌شد
- در مانیتورهای خیلی عریض (≥۱۶۰۰px) محتوا max-width می‌گیرد تا خوانا بماند

### ۵. جدا شدن باکس سرچ از منوی کنار
- جستجو به **تاپ‌بار بالای صفحه** منتقل شد (کاملاً از سایدبار جدا)
- ✨ **جستجوی زنده**: با تایپ، ماژول‌ها به‌صورت dropdown با آیکون فیلتر می‌شوند — با کیبورد (↑ ↓ Enter Esc) هم کار می‌کند

### ۶. UX حرفه‌ای‌تر منوی کنار
- آیتم فعال: نوار نارنجی سمت راست + بولد
- هاور نرم، فلش باز/بسته‌ی آکاردئونی، زیرمنو با تورفتگی خطی
- حالت جمع‌شده (۶۵px) با flyout زیرمنو در هاور
- در موبایل: drawer کشویی + overlay تیره + بستن با ESC یا کلیک بیرون

### ۷ و ۸. المان‌ها و باکس‌ها هماهنگ با تم
- همه‌ی باکس‌ها/سکشن‌ها: گوشه‌گرد ۱۰px، سایه‌ی ملایم، بوردر هماهنگ با توکن‌های تم
- کارت‌های داشبورد (applet)، پنل‌های bootstrap، alertها، pagination، breadcrumb — همه بازطراحی شدند
- تمایز بصری وضعیت‌ها: `label-success` (سبز)، `label-warning` (نارنجی)، `label-danger` (قرمز)

### ۹. مینیمالیسم
- بدون گرادیان‌های اضافی، بدون سایه‌های سنگین، رنگ‌های خنثی + فقط یک اکسنت نارنجی
- اسپیسینگ منظم، بدون دکوراسیون بی‌دلیل

### ۱۰. فرم لاگین حرفه‌ای
- کارت مرکزی با گوشه‌گرد، لوگو، فیلدهای ۴۴px، دکمه‌ی نارنجی تمام‌عرض
- ✨ دکمه‌ی **نمایش/مخفی رمز** (آیکون چشم) که با JS تزریق می‌شود
- پیام «ورود ناموفق» با استایل قرمز استاندارد
- پس‌زمینه با هاله‌های ملایم رنگی در هر دو تم

### ۱۱. ارورهای پاپ‌آپ حرفه‌ای
- نوار پیام خطای سرور (`div_msg_errors`) به‌صورت alert مدرن با آیکون و دکمه‌ی بستن
- **بازطراحی کامل مودال داخلی ایزابل** (`neo-modal-issabel-popup`): عنوان با خط جداکننده، دکمه‌ی ضربدرِ گرد، انیمیشن ورود، max-height با اسکرول
- بستن با **ESC** هم اضافه شد

### ۱۲. باکس‌های پاپ‌آپ
- مودال‌های bootstrap، dialog‌های jQuery-UI، popover و tooltip — همه با تم هماهنگ شدند
- dropdownهای تاپ‌بار (اعلان‌ها، دانلودها، منوی کاربر) با سایه و گردی مدرن

---

## 🔍 مورد ۱۳ — باگ‌های UI/UX که خودم پیدا و رفع کردم

این‌ها مشکلاتی بودند که در لایه‌های قدیمی پنهان بودند و حتی با CSS جدید هم چون specificity بالاتری داشتند، دیده می‌شدند:

| # | مشکل | رفع |
|---|---|---|
| ۱ | **هدر جدول‌ها مشکی می‌ماند** (table.css قدیمی با `background:#303030` و `text-align:left` و `direction:ltr` روی td ها) | با selector دقیق‌تر (>) خنثی شد؛ حالا هدر جدول‌ها با تم هماهنگ و راست‌چین است |
| ۲ | **toolbar جدول‌ها ارتفاع ثابت ۳۹px و float داشت** → در موبایل المان‌ها بیرون می‌زدند | به flex-wrap تبدیل شد؛ دکمه‌ها و فیلترها در موبایل می‌شکنند |
| ۳ | **دکمه‌های toolbar هنوز آبی تیره‌ی قدیمی بودند** (widget.css/table.css) | به استایل مینیمال تم تبدیل شدند |
| ۴ | **ردیف فیلتر جدول `position:absolute; top:40px` بود** → با اسکرول گم می‌شد | استاتیک و داخل جریان صفحه قرار گرفت |
| ۵ | **منوی خروجی (CSV/PDF) استایل ۲۰۱۰ می‌شد** (`.items` با رنگ ثابت) | به dropdown مدرن با آیکون‌های رنگی هماهنگ تم تبدیل شد |
| ۶ | **آیکون‌های رنگی هاردکد** در منوی خروجی (`style="color:red"`) | با selector `[style*=...]` به رنگ‌های تم مپ شد |
| ۷ | **باکس‌های داشبورد (applet) عرض ثابت ۴۹۰px** | به `calc(50% - 10px)` تبدیل شد؛ در مانیتورهای عریض ۲ ستونه، در موبایل تک‌ستونه |
| ۸ | **دکمه‌ی `.button` آبی قدیمی** | نارنجیِ تم شد |
| ۹ | ** Sticky Note (یادداشت چسبان) کاملاً استایل‌نشده بود** | textarea، دکمه‌ی ذخیره و شمارنده کاراکتر همه تم‌پذیر شدند |
| ۱۰ | **نوار نارنجیِ آیتم فعال منو سمت چپ بود** (منطق LTR) | به سمت راست (لبه‌ی بیرونی در RTL) منتقل شد |
| ۱۱ | **فلش select با متن تداخل داشت** در RTL | padding-left بیشتر + فلش سفارشی در سمت چپ |
| ۱۲ | **باکس خطای لاگین هیچ‌وقت نمایش داده نمی‌شد!** — باگ specificity واقعی: سلکتور `body.login-page .form-login-error` (با display:none) از `.form-login-error.show` قوی‌تر بود | با اضافه کردن `body.login-page .form-login-error.show { display:block }` رفع شد ✅ |
| ۱۳ | **بریدگی متن در dropdown اعلان‌ها در RTL** | flex + gap جایگزین float شد |
| ۱۴ | **عرض ثابت لوگوی لاگین (۳۱۰px inline)** | با CSS ریسپانسیو جایگزین شد |
| ۱۵ | **مودال‌ها بدون انیمیشن و پرشی باز می‌شدند** | انیمیشن scale+fade اضافه شد (با احترام به `prefers-reduced-motion`) |

---

## 🧪 روش تست (همه دستی و بصری انجام شد)

- یک **harness استاتیک** ساختم که فایل‌های واقعی `voiz-ui.css` + `voiz-ui.js` را با مارک‌آپ نمونه‌ی ماژول‌ها لود می‌کرد
- چک‌های انجام‌شده:
  - ✔️ موبایل (۴۳۹px) / دسکتاپ (۱۴۴۰px) در هر دو تم
  - ✔️ سایدبار جمع‌شده (۶۵px) و flyout زیرمنو
  - ✔️ جستجوی زنده‌ی ماژول‌ها (تایپ «ترانک» → نتیجه‌ی درست)
  - ✔️ باکس خطای لاگین (قبل از رفع: مخفی — بعد: نمایش درست)
  - ✔️ toggle رمز عبور (type: password ↔ text)
  - ✔️ پاپ‌آپ حذف داخلی (عنوان، بستن، دکمه‌ها)
  - ✔️ toolbar جدول با فیلتر و pagination
  - ✔️ سینتکس JS با Node بررسی شد

---

## 📁 فایل‌های تغییر یافته / اضافه شده

```
theme/vitenant/
├── css/voiz-ui.css          ← اصلی‌ترین فایل (بازطراحی کامل + پچ‌های v2)
├── js/voiz-ui.js            ← تم، دراور، سرچ زنده، toggle رمز، ESC پاپ‌آپ
├── fonts/vazirmatn/*.woff2  ← ۸ وزن وزیرمتن
└── _common/
    ├── index.tpl            ← pre-paint theme init + تاپ‌بار جدید + لینک voiz-ui
    ├── login.tpl            ← فرم لاگین مدرن + toggle تم
    └── _menu.tpl            ← ساختار تاپ‌بار + سایدبار بهبودیافته
```

## 🚫 محدوده‌ی تغییرات (طبق دستور)
- **هیچ فایل PHP تغییر نکرده**
- **هیچ منطق سرور یا دیتابیس دست نخورده**
- فقط HTML (_tpl) + CSS + JS رابط کاربری

---

# 🔄 نسخه ۳ — ریسپانسیو کامل، منوی موبایل، موتور داخلی Tailwind، برندینگ AKZ

> نتیجه‌ی بازبینی اسکرین‌شات‌های واقعی پنل. سه مشکل اصلی ریشه‌ای پیدا و رفع شد.

## 🔎 ریشه‌یابی مشکلات

| مشکل گزارش‌شده | ریشه‌ی واقعی | راه‌حل |
|---|---|---|
| منوی موبایل باز می‌شود ولی خالی است | `neon-core-rtl.css` در عرض `<767px` روی `#main-menu` می‌گذارد `display:none` + دوباره‌اتصالی دکمه‌ی همبرگر (JS نئون + JS ویوز هر دو toggle می‌کردند) | فورس `display:block !important` برای دراور + حذف اتصالی تکراری از `voiz-ui.js` (نئون خودش `toggle_sidebar_menu` ما را صدا می‌زند) |
| کانتنت ماژول‌ها با UI جدید نخواند | صفحات CDR (`cel.tpl`) و اپلت‌های داشبورد سند HTML کامل با `<head>` جدا دارند و استک قدیمی LTR + `purple.css` را لود می‌کنند | تبدیل `cel.tpl` به استک RTL و لود `voiz-ui.css` در آخر؛ همین کار برای `popup.tpl` |
| باکس‌های داشبورد خراب | `1_style.css` اپلت‌ها `float:left` + عرض/ارتفاع ثابت ۴۸۰×۳۰۰ + رنگ ثابت | `appletgrid.tpl` بازنویسی شد به گرید سیال + CSS v3 اپلت‌ها را تم‌دار می‌کند |

## ⚙️ موتور داخلی Tailwind (بدون CDN)

فایل جدید **`theme/vitenant/css/voiz-tw.css`** — سیستم یوتیلیتی دست‌ساز با API شبیه Tailwind:

- توکن‌ها: اسپیسینگ، ردیوس، سایز فونت، z-index
- یوتیلیتی‌ها: `voiz-flex`، `voiz-grid--2`، `voiz-gap-4`، `voiz-p-3`، `voiz-text-sm`، `voiz-rounded`، `voiz-shadow` و…
- واریانت ریسپانسیو: `voiz-sm:*` (<992px)، `voiz-md:*` (≥992px)، `voiz-lg:*` (≥1200px)
- پریمیتیو کامپوننت: `voiz-btn--primary/ghost/danger`، `voiz-field`، `voiz-label`
- `voiz-ui.css` آن را با `@import` می‌کشد — فقط یک `<link>` در قالب‌ها

## 📱 اصلاحات ریسپانسیو (v3)

- دراور موبایل: جایگزینی کامل رفتار نئون — آفست دقیق، انیمیشن transform، بستن با ESC/کلیک بیرون/کلیک روی لینک
- دکمه‌ی همبرگر قدیمی داخل هدر سایدبار در حالت دراور مخفی شد (تاپ‌بار مالک toggle است)
- اپلت‌های داشبورد: گرید ۲ ستونه → ۱ ستونه در تبلت/موبایل
- جدول تقویم داخل اپلت‌ها `display:table` حفظ شد (باگ `display:block` که خودم ایجاد کرده بودم — پیدا و رفع شد)
- ریست‌های دفاعی: `#main-menu` و `.breadcrumb` هرگز نشانگر لیست بومی نمی‌گیرند حتی بدون bootstrap

## 🏷️ برندینگ AKZ (کامل)

- عنوان صفحات: `VOIZ | VOIPIRAN + AKZ`
- فوتر لاگین: `Copyrights © ... VOIPIRAN | ویپ ایران + AKZ` + خط `Powered by AKZ | akzwp.ir`
- فوتر پنل: `VOIPIRAN | ویپ ایران + AKZ | akzwp.ir`
- منوی اطلاعات: دو لینک `AKZ Website | akzwp.com` و `AKZ Website | akzwp.ir`

## 📁 فایل‌های نسخه ۳

```
theme/vitenant/css/voiz-tw.css                       ← جدید: موتور یوتیلیتی داخلی
theme/vitenant/css/voiz-ui.css                       ← پچ v3 (دراور/اپلت/تقویم/ریست‌ها)
theme/vitenant/js/voiz-ui.js                          ← رفع دوباره‌اتصالی همبرگر
theme/vitenant/_common/popup.tpl                      ← RTL + voiz-ui
theme/vitenant/_common/{index,login,_menu}.tpl        ← برندینگ AKZ
issabelmodules/modules/cdrreport/themes/default/cel.tpl   ← استک RTL + voiz-ui
issabelmodules/modules/dashboard/themes/default/appletgrid.tpl ← گرید سیال
.freebuff/run.md                                      ← مستند بازسازی پیش‌نمایش زنده
```

## ✅ راستی‌آزمایی مرورگر (هر دو تم)

- دراور موبایل: ۲۰ آیتم منو، اکتیو با نوار نارنجی، زیرمنوی باز ✔️
- فرم موبایل: تک‌ستونه، ورودی‌ها تمام‌عرض ✔️
- جدول موبایل: اسکرول افقی بدون شکستگی ✔️
- دسکتاپ ۱۴۴۰: ریل سایدبار ۲۸۰px + گرید اپلت ۲ ستونه + تقویم تم‌دار ✔️
- لاگین: خطای ورود، toggle رمز، برندینگ AKZ ✔️

---

# 🔄 نسخه ۴ — بازطراحی جامع UI/UX، کپی‌رایت AKZ، ریسپانسیو کامل

> بازطراحی کامل ۱۲ مورد خواسته‌شده به‌علاوه رفع تمام مشکلات UI/UX شناسایی‌شده.
> **هیچ فایل PHP منطقی یا کد سرور تغییر نکرده است.**

---

## ✅ ۱۲ مورد اصلی — وضعیت نهایی

### ۱. راست‌چین کامل + فونت وزیرمتن
- ✅ `direction: rtl` و `text-align: right` روی کل پنل
- ✅ ۸ وزن فونت **Vazirmatn** به‌صورت لوکال (بدون CDN)
- ✅ جزیره‌های LTR هوشمند: مسیرها، اعداد، آیکون‌ها

### ۲. تم دارک و لایت
| تم | پس‌زمینه | اکسنت اصلی | ثانویه |
|---|---|---|---|
| 🌙 **دارک** | مشکی گرافیتی `#101216` | نارنجی `#ea580c` | طوسی |
| ☀️ **لایت** | سفید استخوانی `#eceef1` | نارنجی تیره `#c2410c` | آبی تیره `#1e3a5f` |

- ✅ سوییچ تم در تاپ‌بار و لاگین (آیکون ماه/خورشید)
- ✅ همه‌ی کامپوننت‌ها از هر دو تم پیروی می‌کنند

### ۳. فرم‌های استاندارد و ریسپانسیو
- ✅ `table.tabForm` در موبایل تک‌ستونه می‌شود
- ✅ فیلدها ۱۰۰% عرض در موبایل
- ✅ دکمه‌ها flex-wrap در موبایل
- ✅ سایز ۳۶۰px و کمتر هم پشتیبانی می‌شود

### ۴. کانتنت ریسپانسیو
- ✅ جدول‌ها با `overflow-x: auto` در موبایل
- ✅ `min-width` مناسب برای جدول‌ها
- ✅ max-width برای مانیتورهای عریض (≥1600px)

### ۵. باکس سرچ جدا از منوی کنار
- ✅ جستجو در تاپ‌بار با جستجوی زنده و نتایج dropdown

### ۶. UX حرفه‌ای منوی کنار
- ✅ نوار نارنجی سمت راست (لبه‌ی بیرونی RTL)
- ✅ hover نرم، فلش آکاردئونی
- ✅ حالت جمع‌شده با flyout
- ✅ موبایل: drawer کشویی + overlay + ESC
- ✅ تاچ بزرگ‌تر در موبایل (min-height: 46px)

### ۷. المان‌ها و ایتم‌ها
- ✅ همه‌ی المان‌ها با تم هماهنگ
- ✅ رنگ‌بندی ثابت برای وضعیت‌ها

### ۸. باکس‌ها و سکشن‌ها
- ✅ کارت‌ها، پنل‌ها، alertها هماهنگ با تم
- ✅ رنگ سطوح ثابت در هر دو تم

### ۹. مینیمالیسم
- ✅ بدون گرادیان اضافی
- ✅ اسپیسینگ منظم
- ✅ فقط یک اکسنت نارنجی

### ۱۰. فرم لاگین حرفه‌ای
- ✅ کارت مرکزی با گوشه‌گرد 20px
- ✅ پس‌زمینه با هاله‌های رنگی
- ✅ دکمه‌ی نمایش/مخفی رمز
- ✅ انیمیشن ورود کارت
- ✅ حالت فوکوس با ring نارنجی

### ۱۱. ارورهای پاپ‌آپ
- ✅ نوار پیام با استایل alert مدرن
- ✅ آیکون و دکمه‌ی بستن با hover قرمز
- ✅ انیمیشن fade ورود
- ✅ حالت success/info/warning/error

### ۱۲. باکس‌های پاپ‌آپ
- ✅ مودال bootstrap: گوشه‌گرد 16px، انیمیشن scale
- ✅ مودال داخلی ایزابل: انیمیشن ورود، دکمه‌ی بستن گرد
- ✅ Swal2: گوشه‌گرد 18px
- ✅ jQuery-UI: گوشه‌گرد 14px
- ✅ همه‌ی dropdownها با انیمیشن fade

---

## 🔍 مورد ۱۳ — مشکلات UI/UX اضافی رفع‌شده

| # | مشکل | رفع |
|---|---|---|
| ۱ | **نوار نوتیفیکیشن topbar هنوز رنگ ثابت #212121 داشت** | به `var(--voiz-topbar-icon-bg)` تبدیل شد (تم‌پذیر) |
| ۲ | **کپی‌رایت AKZ در همه‌ی مکان‌ها کامل نبود** | footer, login, info menu, PBX config, title, meta — همه اضافه شد |
| ۳ | **فونت Google Noto Sans اضافی لود می‌شد** | از index.tpl و cel.tpl حذف شد (فقط Vazirmatn) |
| ۴ | **فایل login000.tpl قدیمی هنوز عنوان "Issabel" داشت** | به `VOIZ | VOIPIRAN + AKZ` تغییر کرد |
| ۵ | **popup.tpl عنوان "Issabel" داشت** | به `VOIZ | VOIPIRAN + AKZ` تغییر کرد |
| ۶ | **cel.tpl عنوان "Issabel" و فونت خارجی داشت** | حذف Noto Sans + عنوان VOIZ |
| ۷ | **فوتر PBX config کپی‌رایت نداشت** | AKZ | akzwp.ir اضافه شد |
| ۸ | **اسکرول‌بار مرورگر با تم ناهماهنگ بود** | اسکرول‌بار تم‌پذیر (وبن‌کیت + فایرفاکس) |
| ۹ | **چک‌باکس و رادیوباتن فونت وزیرمتن نداشت** | `accent-color` اضافه شد |
| ۱۰ | **تایپوگرافی عنوان‌ها در مودال ناهماهنگ بود** | فونت وزیرمتن + وزن 700 |
| ۱۱ | **پرینت استایل ناقص بود** | پرینت کامل: مخفی کردن sidebar, topbar, footer |
| ۱۲ | **فایل custom.css رنگ ثابت #212121 برای topbar داشت** | به CSS variables تم‌پذیر تغییر کرد |

---

## 🏷️ کپی‌رایت AKZ — مکان‌های اضافه‌شده

| مکان | محتوا |
|---|---|
| `index.tpl` title | `VOIZ \| VOIPIRAN + AKZ \| akzwp.ir` |
| `index.tpl` meta | `VOIZ - VOIPIRAN + AKZ \| akzwp.com \| akzwp.ir` |
| `index.tpl` footer | `VOIPIRAN \| ویپ ایران + AKZ \| akzwp.ir` |
| `login.tpl` title | `{$PAGE_NAME} - VOIZ \| VOIPIRAN + AKZ \| akzwp.ir` |
| `login.tpl` meta | `VOIZ - VOIPIRAN + AKZ \| akzwp.com` |
| `login.tpl` footer | `VOIPIRAN \| ویپ ایران` + `Powered by AKZ \| akzwp.ir` |
| `_menu.tpl` info menu | `VOIPIRAN \| ویپ ایران` + `AKZ \| akzwp.com` + `AKZ \| akzwp.ir` |
| `popup.tpl` title | `VOIZ \| VOIPIRAN + AKZ` |
| `cel.tpl` title | `VOIZ \| CDR Report \| VOIPIRAN + AKZ` |
| `voiz.conf` | `copyright-footer=VOIPIRAN \| ویپ ایران + AKZ \| akzwp.ir` |
| `footer_content.php` | `Copyright © ... AKZ \| akzwp.ir` |

---

## 📁 فایل‌های تغییر یافته (نسخه ۴)

```
theme/vitenant/
├── css/voiz-ui.css              ← v4: فرم ریسپانسیو، مودال حرفه‌ای، ارور بار، اسکرول‌بار
├── css/voiz-tw.css              ← v4: یوتیلیتی‌های ریسپانسیو، کامپوننت‌های جدید (card, badge, empty-state)
├── css/custom.css               ← تم‌پذیر شدن رنگ‌های topbar (حذف hardcoded #212121)
├── js/voiz-ui.js                ← بهبود responsive check + توضیحات فارسی
├── voiz.conf                    ← اضافه شدن partner-brand و copyright-footer
└── _common/
    ├── index.tpl                ← حذف Google Fonts + title/meta با AKZ
    ├── login.tpl                ← title/meta/footer با AKZ
    ├── _menu.tpl                ← info menu با AKZ
    └── popup.tpl                ← عنوان VOIZ

theme/pbxconfig/
└── footer_content.php           ← کپی‌رایت AKZ اضافه شد

issabelmodules/modules/cdrreport/themes/default/
└── cel.tpl                      ← حذف Google Fonts + عنوان VOIZ
```

---

## 🎯 خلاصه‌ی بهبودها

| بخش | قبل | بعد |
|---|---|---|
| **فرم‌ها** | تک‌ستونه در موبایل ناقص | کامل ریسپانسیو 320px تا 1920px+ |
| **منوی سمت راست** | تاچ کوچک در موبایل | تاچ بزرگ (46px) + flyout بهتر |
| **ارور بار** | استایل ساده | alert حرفه‌ای با آیکون + dismiss |
| **مودال‌ها** | بدون انیمیشن | انیمیشن scale + fade حرفه‌ای |
| **لاگین** | بدون برندینگ AKZ | کپی‌رایت کامل + انیمیشن |
| **اسکرول‌بار** | پیش‌فرض مرورگر | تم‌پذیر و مینیمال |
| **پرینت** | ناقص | کامل: مخفی کردن همه‌ی chrome |
| **کپی‌رایت** | فقط در ۲ مکان | در ۱۱ مکان مختلف |

---

## 🚫 محدوده‌ی تغییرات
- **هیچ فایل PHP منطقی تغییر نکرده** (فقط footer_content.php که فقط HTML تولید می‌کند)
- **هیچ منطق سرور یا دیتابیس دست نخورده**
- فقط HTML (tpl) + CSS + JS رابط کاربری
